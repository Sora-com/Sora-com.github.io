baseURL = "http://localhost:1313/"
locale = "en-US"
title = "notebook"
disableKinds = ["taxonomy", "term"]

[params]
  author = "Alice"
  description = "this is a place for me to write about the things I think about"
  avatar = "images/avatar.svg"
  theme = "auto"
  homeNotes = 5
  info = [
    "notes, half-baked ideas and things I'm learning.",
    "nothing here is final.",
  ]
  [[params.elsewhere]]
    name = "github"
    url = "https://github.com/Sora-com"

[permalinks]
  posts = "/posts/:slug/"

[markup.goldmark.renderer]
  unsafe = true

[markup.highlight]
  noClasses = false
